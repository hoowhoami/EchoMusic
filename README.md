# EchoMusic

🎉 一个简约的音乐播放器

## 运行步骤

```bash
# 获取依赖
flutter pub get

# 运行 (macOS)
flutter run -d macos

# 运行 (Windows)
flutter run -d windows

# 运行 (Linux)
flutter run -d linux
```
