# Keep this optimization target-scoped: passing an x86 CPU model to LLVM on arm64 can miscompile
# even though LLVM reports that the unrecognized processor is being ignored.
[target.'cfg(target_arch = "x86_64")']
rustflags = ["-C", "target-cpu=x86-64-v3"]
